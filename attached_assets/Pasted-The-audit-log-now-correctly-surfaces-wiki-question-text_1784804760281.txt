The audit log now correctly surfaces wiki.question text as record titles — but this exposed that the app's own internal canned prompts render as verbose machine-speak. RECOMMENDATION_PROMPT in server/brain/proxy.ts (the HomePage "Brain's take" generator) appears in the Audit Log titled with its raw prompt-engineering text ("…Be concrete and numeric; do not greet or add commentary"), which reads as noise to a tenant. User-typed questions should keep showing their text; app-generated prompts should render in human terms.
Changes needed:

Create a shared module importable by both server/ and client/src (e.g. shared/cannedPrompts.ts) containing each canned prompt the BFF sends to brain-core's wiki/question, with three fields per entry: the exact prompt text, a short human title (e.g. "Brain generated your money insight"), and a one-line human description (e.g. "Brain scanned your cash flow, spending, and receivables to pick the single most important item for your dashboard"). Move RECOMMENDATION_PROMPT there and import it in proxy.ts so the sent text and the recognized text can never drift apart.
In the client's audit mapping (wikiQuestionText / the wiki.question summary in brainAudit.ts), check inputs.question against the shared canned-prompt list by exact match. On match, use the entry's human title as the record title (list card and popup heading) instead of the raw prompt text.
In AuditRecordPopup.tsx, for matched canned prompts: show the human description as the lifecycle body, change the actor line from "Asked on behalf of {actor}" to "Run automatically on behalf of {actor}" (it's system-initiated, not user-asked), and show the exact raw prompt beneath it as smaller secondary text labeled "Exact prompt used" — the audit log should never hide what was actually sent, just stop leading with it.
Leave unmatched (user-typed) questions rendering exactly as they do now.

Scope: the new shared module, the RECOMMENDATION_PROMPT import in server/brain/proxy.ts, brainAudit.ts title mapping, and AuditRecordPopup.tsx. No changes to brain-core calls, classification logic, or other pages.