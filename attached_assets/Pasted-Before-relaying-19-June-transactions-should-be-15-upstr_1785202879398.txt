Before relaying "19 June transactions should be 15" upstream to brain-core as a bug: Codex's verification on d79dd204fe82e8a93f0a90fdbc5ca0ce47bb644e independently confirmed 19 transactions with a net of -14586.02 for the bank statement fixture — same document, same tenant type, verified end to end via canonical_transaction rows with correct source/evidence IDs. Your Step 2/3 verification just now shows 15 for what should be the same underlying file.

That's not a rounding or timing difference — it's a real inconsistency, and there's a likely explanation on this side worth ruling out first: did running generate-demo-seed.ts to add the crypto wallet and tax return generators also regenerate the existing bank_statement_2026-06.pdf (and/or AR aging/payroll), rather than only adding the two new files in isolation? If the script regenerates all seed files from one run, the bank statement's actual content may have quietly changed as a side effect of this change — which would fully explain a transaction-count difference without brain-core's counting being wrong at all, given it was independently verified against what it received.

Please check:

Diff or hash server/assets/demo-seed/bank_statement_2026-06.pdf (and AR aging/payroll) against what was there before this change — did the file itself change?
If it did change: is that intentional (e.g., you regenerated all five for consistency), or an unintended side effect of the generator script? If intentional, the "19 vs 15" discrepancy isn't a bug to report upstream at all — it's just an expected consequence of a new fixture, and Codex's earlier verification was simply against the old file.
If the file is unchanged: then this is worth relaying to brain-core as a real discrepancy, with both counts and confirmation the file is byte-identical to what was verified.

Only send the "19 should be 15" note upstream after this is resolved either way — don't want to send brain-core chasing a bug that's actually just an unregenerated fixture on this side.