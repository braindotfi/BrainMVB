Continue on a new branch (git fetch && pull main first):
feature/proposal-cards-full-parity

CONTEXT
brain-core PR #384 (merged, deployed, and now verified via live HTTP smoke
test against real prod proposal rows) expanded GET /v1/proposals with
additive fields: details, policy, presentation, available_decisions, and
stored_action_type — confirmed present with real values across
fraud_anomaly, cash_forecast, treasury, subscription, and compliance
proposal types, and available for all other types per the same schema.
Reference docs: docs/contracts/proposals-read-model.md in brain-core, and
the updated docs.brain.fi Wiki/Policy API pages.

KNOWN DATA QUIRK — handle this explicitly
policy.policy_id is null on most verified rows even though the rest of the
policy object is populated (e.g. policy.decision: "confirm", trace data,
and sometimes a separate policy.matched_rule_id like "cmp_policy_violation"
alongside the null policy_id). Do not build the "Flagged by <policy_id>"
line assuming policy_id is always present:
- If policy_id exists, use it.
- Else if matched_rule_id exists, use that instead.
- Else if policy.decision or other policy content exists, render a
  fallback explanation from that content rather than an empty/broken line.
- Only omit the "Flagged by" line entirely if policy has no usable
  identifier or explanation at all.

TASK

1. Read docs/contracts/proposals-read-model.md and the updated API docs
   first to get the exact shape of details, policy, presentation, and
   available_decisions per proposal type — don't guess field names from
   the UI alone.

2. Extend the shared proposal card component (already used for Invoice
   Agent, Cash Agent, and Collections) to every proposal_type, binding
   each section to the new fields:
   - Headline recommendation + subline — from presentation
   - "Why This Needs Your Call" narrative — from details/narrative
   - Structured key-facts table — from details, styled like Invoice's
     Vendor/Trailing Avg/This Invoice/Variance rows
   - Evidence tiles — resolved via Wiki page_types referenced in
     evidence/evidence_refs, never raw wiki:/ URIs or cp_/inv_/obl_/etc.
     IDs in the primary view
   - Confidence bar — "High/Medium/Low · XX%" format
   - "What Happens Next" / "If This Is Wrong" — from presentation/policy
     if present; omit the section (don't fabricate) if a given type's
     payload doesn't include it
   - "Flagged by ..." line — per the fallback logic above
   - Action buttons — driven by available_decisions per proposal, not a
     hardcoded Reject/Approve pair
   - Collapsed "Technical Detail" — six-layer JSON, sourced from the
     richer response

3. Visual styling must literally reuse the Invoice/Cash Agent component
   markup and CSS — same typography, spacing, card treatment, section
   dividers, button styling — across every proposal type.

4. For the newly-promoted advisory types (personal_budget, tax_prep,
   travel_finance, etc.), route them into the Inbox using
   stored_action_type / type as the routing key, and confirm they render
   through the same shared card rather than falling through to a default/
   fallback view.

5. Verify in the live preview using the 5 confirmed-pending proposals for
   tnt_01KYS8R54VDRSW6ND3GN2649T0 (flag_transaction/fraud_anomaly,
   cash_forecast, treasury, flag_subscription/subscription,
   notify/compliance) — confirm every section renders with real data,
   correct action buttons per available_decisions, correct "Flagged by"
   fallback behavior, and no raw IDs or placeholder text anywhere in the
   primary view. For proposal_types with no live example available, use a
   synthetic one and note that in your report.

Update done/pending notes and CLAUDE.md with the final shared card
component's full field contract and the complete proposal_type list, so
any future proposal_type is built against this pattern from day one.