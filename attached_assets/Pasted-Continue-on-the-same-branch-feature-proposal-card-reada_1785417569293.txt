Continue on the same branch (feature/proposal-card-readability-and-formatting).

The proposal modal now has correct currency formatting and a collapsed
"Technical reference" section — good. But the core redesign is incomplete:
the card still just shows the raw description paragraph with no resolved
company header or structured detail rows.

Find the same proposal modal component (the one rendering "Collections",
"Proposed by [agent]", the confidence bar, and the description text) and
add the following, inserted between the description paragraph and the
"Technical reference" section:

1. A resolved-entity header block, above or replacing the current plain
   description opening:
   - Small circular avatar with the counterparty's initials
   - Counterparty display name (e.g. "Midmarket Solutions") as the primary
     line
   - Invoice number + amount as a secondary line underneath (e.g.
     "AR-MIDMARKET-001 · $42,000.00")
   - Pull this from whatever the backend already resolves the counterparty
     name from (same lookup used to populate the description sentence,
     since "Midmarket Solutions" already appears there) — don't hardcode it,
     bind it to the actual proposal payload field.

2. A structured detail row table below the description, using whatever
   fields already exist on the underlying invoice/collections record:
   - Due date
   - Reminder history (e.g. count of reminders sent this billing period —
     only include this row if that data exists in the record; don't fabricate
     it if it's not tracked)
   - Recipient email the reminder will be sent to
   Each row: label + Tabler icon on the left, value right-aligned, thin
   top border between rows — same visual pattern as the existing
   "Technical reference" rows already in the card, just not collapsed.

3. A collapsible "Message preview" section (collapsed by default, same
   `<details>` pattern as "Technical reference") showing the actual
   reminder text that will be sent if approved — pull this from the
   proposal's generated message content if the backend already generates
   it before approval; if it doesn't currently exist as a field on the
   payload, add it so the approver can see exactly what goes out before
   clicking Approve.

Keep the existing confidence bar, priority badge, and Reject/Approve
buttons as they are — only inserting the new header block, detail table,
and message preview in the body.

Apply the same structure to the Invoice, Cash, and Close agent proposal
modals using whatever equivalent fields exist in their respective payloads
— don't leave Collections as the only one with the richer layout.

Update the running done/pending notes and CLAUDE.md to reflect the final
data-shape convention (resolved name + avatar, detail rows, message
preview, collapsed technical IDs) once this is in place.