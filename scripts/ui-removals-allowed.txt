# Declared UI removals.
#
# scripts/check-removed-ui.mjs fails when a data-testid / testId that exists on
# the base ref exists nowhere in your branch. That is deliberate: three separate
# "sync to main" merges silently deleted finished UI because a stale copy of a
# file rode along in a large diff, and no check ever noticed.
#
# Removing UI is fine. It just has to be a decision somebody wrote down.
# One id per line, followed by the reason it is gone.
#
#     <test-id>    <why it was removed>
#
# If you are adding a line here only to make the check pass, stop -- that is the
# exact failure this file exists to prevent. Confirm the control is genuinely
# superseded before declaring it.

banner-overview-incomplete   superseded — Overview aggregate now waits for all contributing feeds and shows an explicit failure state instead of a partial-data banner
filter-priority              superseded — Inbox restructure replaced the priority filter with outcome-bucketed sections (needs input / awareness / decided)
row-overview                 superseded — Overview tier-row list replaced by the cash projection + KPI dashboard layout
text-decision-count          superseded — decided-count pill replaced by per-section counts in the three-section Inbox
button-missing-evidence-dismiss   removed — Needs Your Input modal now offers only Remind later and the field-specific fix action
connect-wallet-btn   removed — frontend wallet runtime and its unused connect control were retired with the Coinbase Wallet cleanup
button-avatar   superseded — Settings Profile no longer has a top avatar record; Identity uses a placeholder profile icon
button-edit-profile   superseded — the old top profile editor was replaced by the Name row's inline edit action
img-avatar   superseded — Settings Profile no longer renders an uploaded avatar in the removed top record
input-avatar-file   superseded — avatar upload controls were removed from Settings Profile
button-close-invoice-preview   removed — Invoice Record no longer exposes a thumbnail preview overlay
invoice-preview-overlay   removed — Invoice Record no longer exposes a thumbnail preview overlay
invoice-thumbnail   removed — Invoice Record header now matches Bill Details without an invoice thumbnail
link-settings-billing   superseded: Developers now displays the server-owned rate entitlement instead of a local billing-plan selection
