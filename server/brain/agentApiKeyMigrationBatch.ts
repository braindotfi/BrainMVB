/**
 * Phase 3 rollout manifest. Keep this empty in the capability-only release.
 * Add at most five tenant ids per reviewed batch.
 */
export const AGENT_API_KEY_MIGRATION_BATCH: readonly string[] = [];
